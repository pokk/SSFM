<?xml version="1.0" encoding="utf-8"?>
<manifest package="taiwan.no1.app.ssfm"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          tools:ignore="MissingLeanbackLauncher">

    <uses-feature
        android:name="android.software.leanback"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".mvvm.views.activities.SplashActivity"
            android:theme="@style/AppTheme.Splash">
            <!--<intent-filter>-->
            <!--<action android:name="android.intent.action.MAIN"/>-->
            <!--<category android:name="android.intent.category.LAUNCHER"/>-->
            <!--</intent-filter>-->
        </activity>
        <activity android:name=".mvvm.views.activities.MainActivity">
        </activity>
        <activity android:name=".mvvm.views.activities.IndexActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".mvvm.views.activities.PlayMainActivity">
        </activity>
        <activity android:name=".mvvm.views.activities.PreferenceActivity">
        </activity>
        <activity android:name=".mvvm.views.activities.TestActivity">
        </activity>
    </application>

</manifest>