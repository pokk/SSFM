<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".mvvm.views.activities.SearchActivity">

    <data>

        <variable
            name="avm"
            type="taiwan.no1.app.ssfm.mvvm.viewmodels.RecyclerViewArtistChartViewModel"/>
    </data>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <android.support.v7.widget.CardView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{avm::artistOnClick}">

            <ImageView
                android:layout_width="@dimen/rv_artist_length"
                android:layout_height="@dimen/rv_artist_length"
                android:layout_gravity="fill"
                android:error="@{@drawable/sample_jieyi_icon}"
                android:imageUrl="@{avm.thumbnail}"
                android:placeHolder="@{@drawable/sample_lady_gaga}"
                android:scaleType="fitXY"
                tools:src="@drawable/sample_lady_gaga"/>
        </android.support.v7.widget.CardView>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:orientation="vertical"
            android:paddingTop="@dimen/one_unit">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxLines="1"
                android:text="@{avm.artistName}"
                tools:text="Lady Gaga"/>

            <TextView
                android:id="@+id/tv_play_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:drawablePadding="@dimen/one_unit"
                android:drawableStart="@drawable/lb_ic_thumb_up_outline"
                android:gravity="center_vertical"
                android:text="@{avm.playCount}"
                tools:text="123456789"/>
        </LinearLayout>
    </LinearLayout>
</layout>
